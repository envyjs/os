var content = document.createElement("div");content.innerHTML = `<div id="rescue"></div>`;document.getElementById("userland").appendChild(content);const terminal = document.getElementById('rescue');document.body.style.backgroundColor = "#1e1e1e";const commands = {help: () => {createOutputLine("Envy Rescue NoGUI Terminal");createOutputLine("(clear, cls),(exec, start), help");},exec: (args) => {if (args.length === 0) {return "Usage: exec <app_path>";}const scriptPath = args[0];loadScript(scriptPath);},start: (args) => {if (args.length === 0) {return "Usage: start <app_path>";}const scriptPath = args[0];loadScript(scriptPath);},clear: () => { terminal.innerHTML = ''; },cls: () => { terminal.innerHTML = ''; },};const createOutputLine = (text, cssClass = 'output') => {const line = document.createElement('div');line.textContent = text;line.className = cssClass;terminal.appendChild(line);};const createInputLine = () => {const inputWrapper = document.createElement('div');inputWrapper.className = 'input';const prompt = document.createElement('span');prompt.textContent = 'rescue>';prompt.className = 'prompt';const input = document.createElement('input');input.type = 'text';input.className = 'ti';inputWrapper.appendChild(prompt);inputWrapper.appendChild(input);terminal.appendChild(inputWrapper);input.focus();input.addEventListener('keydown', (e) => {if (e.key === 'Enter') {const commandLine = input.value.trim();processCommand(commandLine);inputWrapper.remove();createInputLine();}});};const processCommand = (commandLine) => {if (!commandLine) return;const [command, ...args] = commandLine.split(' ');createOutputLine(`rescue> ${commandLine}`, 'prompt');if (commands[command]) {const result = typeof commands[command] === 'function'? commands[command](args): commands[command];if (result !== undefined) createOutputLine(result);} else {createOutputLine(`"${command}" is not a known command`);}};createOutputLine("Envy Rescue Terminal [Version 10.0.5122], Copyright (C) Envy Group 2022-2027");createInputLine();